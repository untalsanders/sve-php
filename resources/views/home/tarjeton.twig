{% extends "templates/app.twig" %}

{% block title %}
    {{ titlePage | capitalize }}
{% endblock %}

{% block content %}
    <h4>Estudiante: {{ estudiante.nombres }} {{ estudiante.apellidos }}</h4>
    <h2>{{ institucion.institucion | upper }}</h2>
    <h3>{{ institucion.descripcion | upper }}</h3>
    <form action="" method="post">
        <div>{{ "TARJETÓN ELECTORAL" | upper }}</div>
        <ul>
            {% for categoria in categorias %}
                <li>
                    <span>{{ categoria.descripcion }}</span>
                    <ol>
                        {% for candidato in candidatos %}
                            {% if categoria.id == candidato.representante  %}
                                <li>
                                    <input type="radio" name="candidato_a_{{ categoria.nombre | lower }}">
                                    <span>{{ candidato.nombres | capitalize }} {{ candidato.apellidos | capitalize }}</span>
                                </li>
                            {% endif %}
                        {% endfor %}
                    </ol>
                </li>
            {% endfor %}
        </ul>
    </form>
    
{% endblock %}
